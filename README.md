# Pelgulinna tunniplaan

Käivitamine PowerShellis:

```powershell
npm.cmd install
npm.cmd run import
npm.cmd run dev
```

Ava brauseris:

```txt
http://localhost:3000
```

XML fail peab olema siin:

```txt
data/incoming/untis.xml
```
